export enum Filters {
    active = 'active',
    archived = 'archived',
    new_projects = 'new_projects',
    submitted = 'submitted',
    in_progress = 'in_progress',
}

export enum AgencyFilters {
    new_projects = 0,
    submitted,
    in_progress,
}
