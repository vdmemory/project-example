import styled from '@emotion/styled';

export const StyledHeaderPopup = styled.div`
    display: flex;

    button {
        border: none;
        background: none;
        position: absolute;
        cursor: pointer;
        z-index: 5;
        top: 0;
        right: 0;
        padding: 0;
    }
`;
