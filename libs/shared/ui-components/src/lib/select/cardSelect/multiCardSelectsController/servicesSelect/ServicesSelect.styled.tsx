import styled from '@emotion/styled';
import { motion } from 'framer-motion';

export const StyledServicesSelect = styled(motion.section)`
    display: flex;
    flex-direction: column;
    min-height: 452px;
`;
